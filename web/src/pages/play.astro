---
// Legacy bookmark redirect: /play used to host paris-metro solo before the
// multi-domain refactor. Funnel any incoming request to the default-domain
// equivalent so social shares and existing bookmarks stay alive.
import { DEFAULT_DOMAIN_ID } from "../lib/domains";
return Astro.redirect("/" + DEFAULT_DOMAIN_ID + "/play", 301);
---
